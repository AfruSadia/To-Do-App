import 'package:flutter/material.dart';

class KColor {
  static final Color k_bg = Color(0xFFfe5c45);
  static final Color k_ftext = Color(0xFFcac9cf); // form text color
  static final Color k_white = Color(0xFFFFFFFF);
  // TODO :: rename color variables according to standard convention
  /// No need of k prefix here
  /// follow lower camelCase
  /// for hexa color codes use some color code to name generator site for names
  /// use meaningful variable names at all timea
  static final Color black = Color(0xFF000000); //label and bottom text
  static final Color borderOutline = Color(0xFFc9c9c9);
}
